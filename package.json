{
  "name": "cy-http-api",
  "version": "1.0.0",
  "description": "Testing Http API httpbin.org",
  "main": "index.js",
  "scripts": {
    "cypress:open:chrome": "npx cypress run -b chrome --headed",
    "cypress:open:firexof": "npx cypress run -b firefox --headed",
    "cypress:run:chrome": "npx cypress run -b chrome",
    "cypress:run:firefox": "npx cypress run -b firefox",
    "cypress:open:chrome:alltests": "npx cypress run -b chrome --spec cypress/e2e/",
    "cypress:open": "npx cypress open"
  },
  "author": "Kamil Barszczewski",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.3"
  }
}
